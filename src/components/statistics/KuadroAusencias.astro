---
    const { title = "Ausencias" } = Astro.props;
    const { amount = 33 } = Astro.props;
---

<div class="bg-[#d1113a] rounded-2xl p-6 w-[260px] h-[210px] flex flex-col justify-evenly items-end shadow-md">
  <p class="text-gray-200 text-2xl">{title}</p>
  <p class="text-gray-200 font-semibold text-5xl leading-none">{amount}</p>
</div>